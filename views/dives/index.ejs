<%- include('../partials/header') %>

<div class="content-body">

    <img class="head-img"src="https://i.imgur.com/77SOMnK.png">

    <h1 id="index-h1">D I V E &nbsp L O G</h1>

    <form id="add-new-dive"action="/dives/new">
        <input class="style-button" type="submit" value="add new dive">
    </form>

    <h2>my dives</h2>

    <table>
        <thead>
            <th>Dive Number</th>
            <th>Date</th>
            <th>Dive Site</th>
            <th>Details</th>
        </thead>
        <tbody>
            <% dives.forEach(function(d) { %>
                <% if (user && user._id.equals(d.user)) { %>
                    <tr>
                        <td><%= d.number%></td>
                        <td><%= d.date%></td>
                        <td><%= d.site%></td>
                        <td><a class ="dive-details" href="/dives/<%= d._id %>">view</a></td>
                    </tr>
                <% }
                }); %>
        </tbody>
    </table>

</div>

<footer>
    <%- include('../partials/footer'); %>
</footer>
